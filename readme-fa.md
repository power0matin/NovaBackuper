<div align="center">  
  <img src="https://github.com/user-attachments/assets/16cc16e2-f1e5-4ae8-9b5f-bbea33fa39bd" alt="لوگوی NovaBackuper" />  
</div>

# NovaBackuper چیه؟

**NovaBackuper** یک اسکریپت بکاپ‌گیری سبک و حرفه‌ای برای **پنل x-ui** هست که دیتابیس x-ui رو فشرده می‌کنه و به صورت خودکار برای شما توی **تلگرام** ارسال می‌کنه.  
نصبش تعاملیه (ویزارد‌ی)، و در نهایت برای شما اسکریپت بکاپ + کران‌جاب می‌سازه تا بکاپ‌ها به صورت خودکار انجام بشن.

## پلتفرم پشتیبانی‌شده

- [x] **Telegram** (با استفاده از bot token و chat ID)

## ویژگی‌های کلیدی

- **نصاب تعاملی (Wizard)**  
  در حین نصب از شما می‌پرسه:

  - نام/توضیح بکاپ (Remark)
  - بازه زمانی بکاپ‌گیری (دقیقه‌ای / ساعتی با cron)
  - Bot Token و Chat ID تلگرام

- **تمرکز روی x-ui**  
  بکاپ‌گیری از فایل‌های اصلی دیتابیس x-ui:

  - `/etc/x-ui/x-ui.db`
  - `/etc/x-ui/x-ui.db-wal`
  - `/etc/x-ui/x-ui.db-shm`

- **زمان‌بندی خودکار**

  - ساخت اسکریپت اختصاصی بکاپ در مسیر `/root/_<remark>_backuper_script.sh`
  - تنظیم خودکار **cron job** بر اساس بازه زمانی‌ای که انتخاب می‌کنید

- **مدیریت امن فایل‌ها**

  - فشرده‌سازی با `zip`
  - پاک‌سازی چانک‌های قدیمی مربوط به همون remark قبل و بعد از هر بکاپ

- **گزارش کامل در تلگرام**

  - کپشن HTML حرفه‌ای شامل:
    - تاریخ، ساعت و منطقه زمانی
    - IP سرور و Hostname
    - Backup ID
  - ارسال مستقیم به چت تلگرام/گروه مورد نظر شما

- **پشتیبانی از چند توزیع لینوکس**
  - تشخیص خودکار پکیج منیجر (`apt`, `dnf`, `yum`, `pacman`)
  - نصب اتوماتیک ابزارهای مورد نیاز (`curl`, `zip`, `cron` و …)

## قالب‌های پشتیبانی‌شده

NovaBackuper عمداً مینیمال و تخصصی طراحی شده:

- [x] **پنل x-ui** (دیتابیس SQLite در مسیر `/etc/x-ui`)

هم‌چنین در مراحل نصب می‌تونید **دایرکتوری‌های دلخواه** رو اضافه یا حذف کنید تا همراه دیتابیس، مسیرهای دیگه هم داخل فایل بکاپ قرار بگیرن.

> [!NOTE]  
> NovaBackuper بر اساس پروژه‌ی [Backuper](https://github.com/erfjab/Backuper) توسعه داده شده و به نسخه‌ای متمرکز روی **x-ui + Telegram** تبدیل شده.  
> از **@ErfJabs** برای ایده و پایه‌ی اولیه پروژه قدردانی می‌کنیم.

## نصب

برای نصب آخرین نسخه، این دستور رو اجرا کنید:

```bash
sudo bash -c "$(curl -sL https://github.com/power0matin/nova-backuper/raw/master/nova-backuper.sh)"
```

این اسکریپت کارهای زیر رو انجام می‌ده:

1. آپدیت پکیج‌های سیستم
2. نصب وابستگی‌های لازم
3. اجرای ویزارد نصب NovaBackuper
4. ساخت اسکریپت بکاپ در `/root/`
5. اجرای اولین بکاپ به صورت خودکار
6. ساخت cron job برای بکاپ‌گیری خودکار در بازه زمانی دلخواه شما

## استفاده (خلاصه)

بعد از نصب، معمولاً اسکریپت ساخته‌شده این شکلیه:

```bash
/root/_<remark>_backuper_script.sh
```

کران‌جاب هم شبیه این خواهد بود (مثلاً هر ۳۰ دقیقه):

```cron
*/30 * * * * /root/_myxui_backuper_script.sh
```

شما می‌تونید:

- برای ویرایش یا حذف کران‌جاب:

  ```bash
  crontab -e
  ```

- برای اجرای دستی بکاپ:

  ```bash
  bash /root/_<remark>_backuper_script.sh
  ```

## 💙 حمایت از پروژه

اگر NovaBackuper برات مفید بود، یک **ستاره (⭐)** روی ریپو، بهترین حمایته.
ممنون که ازش استفاده می‌کنی 🙏

🔹 توسعه و نگهداری توسط [@power0matin](https://github.com/power0matin)

[![Stargazers over time](https://starchart.cc/power0matin/nova-backuper.svg?variant=adaptive)](https://starchart.cc/power0matin/nova-backuper)
